# Release Process

Release gates are strict by default.

Release is blocked by:

- critical review items
- missing critical dependencies
- critical project health
- missing Fast Lane
- critical storage or source knowledge health

Warnings do not block unless configured later.

Release gates must not run broad project-wide test suites.

