# Legacy Static JavaScript UI

This folder contains the previous browser-only interface.

It is retained as a migration reference only. The primary application is now the Python-rendered interface served by:

`python -m backend.electional.server`

Primary app URL:

`http://127.0.0.1:8765/`

Do not add new features here unless we intentionally need to compare behavior against the retired UI.
