# Backup and Restore

Backups include `data/reliability/`, `data/profiles/`, `data/watchlists/`, and `data/objective_packs/`.

Restore supports dry-run first and does not overwrite existing files unless explicitly allowed.
